{
  "name": "apis-testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "npx cypress open",
    "cy:open:filter:grep": "npx cypress open --env grep=@healthcheck",
    "cy:run": "npx cypress run",
    "cy:run:healthcheck": "npx cypress run --env grep=@healthcheck",
    "cy:run:contract": "npx cypress run --env grep=@contract",
    "cy:run:functional": "npx cypress run --env grep=@functional"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^7.3.0",
    "cy-spok": "^1.5.2",
    "cypress": "^5.3.0",
    "cypress-select-tests": "^1.7.1"
  }
}
